/**
 * Device detection / mobile redirect - DISABLED
 *
 * The site now uses a single codebase with responsive design (styles.css).
 * No redirects to /mobile/. This file is kept as a no-op in case any
 * cached page or external link still loads it.
 */
(function() {
  // No-op: do not redirect. Responsive layout handles all viewports.
})();
